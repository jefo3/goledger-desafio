export const colourStyles = {
  control: ({ ...props }) => ({
    ...props,
    with: '100%',
    borderColor: 'rgb(209 213 219)',
    borderWidth: '1px',
    borderRadius: '0.3rem',
    padding: '0.5rem',
    outline: 'none',
    boxShadow: 'none',
  }),
}
